import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function ProfileInventoryList({item}) {
  return (
    <View>
      <Text>ProfileInventoryList</Text>
    </View>
  )
}

const styles = StyleSheet.create({}) 